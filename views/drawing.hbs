
<!DOCTYPE html>
<html>
  <head>
    <title>{{title}}</title>
    <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.1.1/css/bootstrap.min.css" integrity="sha384-WskhaSGFgHYWDcbwN70/dfYBj47jz9qbsMId/iRN3ewGhXQFZCSftd1LZCfmhktB" crossorigin="anonymous">
    <link rel='stylesheet' href='/stylesheets/style.css' />
  </head>
  <body>
    {{>header}}
    <div class="container">
        <div class="row">
            <div class="col">
                <img src="{{{imageURL}}}" draggable="false">
                <h1>{{{title}}}</h1>
                <p>By: {{publicArtist}}</p>
                <p>Created at: {{creationDate}}</p>
                {{#if rating}}
                    <p>Rating: {{rating}}</p>
                {{else}}
                    <p>No Ratings yet</p>
                {{/if}}
                {{#if desc}}
                    <p>Description: {{{desc}}}</p>
                {{/if}}
            </div>
        </div>
        <div class="row">
            <div class="col">
                {{#if listComments}}
                    {{#each listComments}}
                        <div class="commentDiv rounded">
                            <p>{{authorID.displayName}}</p>
                            {{!-- {{#if authorId}}
                                <p>{{{authorId}}}</p>
                            {{else}}
                                <p>Anonymous</p>
                            {{/if}} --}}
                            <p>Created at: {{creationDate}}</p>
                            <p>{{{desc}}}</p>
                        </div>
                    {{/each}}
                {{else}}
                    <p>No Comments yet</p>
                {{/if}}
                {{#if user}}
                    <field>
                        <p>Leave a comment</p>
                        <form action="comment" method="post" autocomplete="off" id="commentForm">
                                <textarea type="text" name="comment" placeholder="Leave a comment here. 140 Characters Max. Don't be a dick." maxlength="140" required></textarea><br/>
                                <input type="text" name="drawingID" value="{{{id}}}" hidden required/>
                                <button class="btn btn-primary" >Submit Comment</button>
                        </form>
                    </field>
                {{else}}
                    <p>Please <a href="/auth">Login</a> to comment on this post.</p>
                {{/if}}
            </div>
        </div>

        {{!-- TODO tags currently cant be hidden as the hbs thinks the array of empty strings should be rendered}--}
        {{!-- {{#each tags}}
            <p>Tags:{{{tags}}}</p>
        {{else}}
        {{/each}} --}}
    </div>
    {{>footer}}
    <script src="https://code.jquery.com/jquery-3.3.1.slim.min.js"crossorigin="anonymous"></script>
    <script src="https://cdnjs.cloudflare.com/ajax/libs/popper.js/1.14.3/umd/popper.min.js" integrity="sha384-ZMP7rVo3mIykV+2+9J3UJ46jBk0WLaUAdn689aCwoqbBJiSnjAK/l8WvCWPIPm49" crossorigin="anonymous"></script>
    <script src="https://stackpath.bootstrapcdn.com/bootstrap/4.1.1/js/bootstrap.min.js" integrity="sha384-smHYKdLADwkXOn1EmN1qk/HfnUcbVRZyYmZ4qpPea6sjB/pTJ0euyQp0Mk8ck+5T" crossorigin="anonymous"></script>
  </body>
</html>
